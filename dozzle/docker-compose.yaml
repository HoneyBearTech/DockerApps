services:
  dozzle:
    container_name: dozzle
    image: amir20/dozzle:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      # Maps the host port from your .env file to container port 80
      - "${HOST_PORT}:8080"
